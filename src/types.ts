/**
 * string: simple text line as input
 * number: number input (for int or whatever use resolver)
 */
export const Types = {
  string: 'string',
  number: 'number',
  bool: 'bool',
  date: 'date',
  object: 'object',
  file: 'file'
}